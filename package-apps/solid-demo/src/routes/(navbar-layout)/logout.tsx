export default function LogoutPage() {
	// This page exists for convenience during dev if the ui is in a bad state and
	// we need to force a logout.
	return <div>Need to handle automatic logout</div>
}
